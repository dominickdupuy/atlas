"""Composition roots. The only modules allowed to wire adapters into ports
across contexts (D18): `container` for the parent (serve), `child` for the
execute-job subprocess, `connectors_factory` shared by both."""
